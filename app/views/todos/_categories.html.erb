  <div class="flex flex-wrap gap-2 min-h-6 mt-2">
    <% categories.each do |category| %>
      <span class="px-3 py-1 rounded-full text-sm font-medium bg-slate-100 text-slate-500 text-xs">
        <%= category.name %>
      </span>
    <% end %>
  </div>
